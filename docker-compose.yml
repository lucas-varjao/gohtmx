# Auto-generated docker-compose.yml file.
# See https://gowebly.org for more information.

version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: gohtmx
      POSTGRES_PASSWORD: gohtmx
      POSTGRES_DB: gohtmx
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - gowebly_gin_network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U gohtmx -d gohtmx']
      interval: 5s
      timeout: 5s
      retries: 5

  gowebly_gin:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - '7000:7000'
    environment:
      BACKEND_PORT: 7000
      DATABASE_DSN: 'host=postgres user=gohtmx password=gohtmx dbname=gohtmx port=5432 sslmode=disable TimeZone=UTC'
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - gowebly_gin_network

networks:
  gowebly_gin_network:

volumes:
  postgres_data:
